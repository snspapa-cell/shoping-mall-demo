# ============================================
# Heroku 배포 시 Config Vars에 설정할 환경변수
# ============================================

# MongoDB Atlas URL (필수)
MONGODB_ATLAS_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT 설정 (필수)
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
JWT_EXPIRE=7d

# 클라이언트 URL (CORS 허용, Vercel 배포 후 설정)
CLIENT_URL=https://your-vercel-app.vercel.app

# 환경 설정
NODE_ENV=production

# ============================================
# 로컬 개발 시 설정 (선택)
# ============================================
# MONGODB_URI=mongodb://localhost:27017/shopping-mall
# PORT=5000
# NODE_ENV=development
